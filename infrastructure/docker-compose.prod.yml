version: "3.8"

services:
  # Vision service (Java Spring Boot)
  vision:
    image: ${ECR_REGISTRY:-your-registry}/texas-vision:${IMAGE_TAG:-latest}
    restart: always
    networks:
      - texas-network
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - AWS_REGION=eu-central-1
      - SPRING_DATA_MONGODB_URI=mongodb+srv://${MONGO_USER}:${MONGO_PASSWORD}@cluster0.x9mmmer.mongodb.net/vision?retryWrites=true&w=majority

  # Ultron service (NestJS)
  ultron:
    image: ${ECR_REGISTRY:-your-registry}/texas-ultron:${IMAGE_TAG:-latest}
    restart: always
    networks:
      - texas-network
    environment:
      - NODE_ENV=production
      - AWS_REGION=eu-central-1
      - DOMAIN=.allinrange.com
      - UI_URL=https://www.allinrange.com
      - POSTGRES_PORT=5432
      - POSTGRES_DB=ultron

  # Quickview frontend
  quickview:
    image: ${ECR_REGISTRY:-your-registry}/texas-quickview:${IMAGE_TAG:-latest}
    restart: always
    environment:
      - NODE_ENV=production
      - REACT_APP_ULTRON_API_URL=https://allinrange.com/api
      - REACT_APP_VISION_API_URL=https://allinrange.com/vision
    depends_on:
      - ultron
      - vision
    networks:
      - texas-network

  # Nginx reverse proxy
  nginx:
    image: nginx:alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infrastructure/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./infrastructure/nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - quickview
      - ultron
      - vision
    networks:
      - texas-network

networks:
  texas-network:
    driver: bridge
