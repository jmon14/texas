/**
 * Category Enum
 * Type-safe category definitions for scenario grouping
 * Used for filtering and organizing scenarios in the UI
 */
export enum Category {
  OPENING_RANGES = 'Opening Ranges', // Opening range scenarios
  THREE_BETTING = '3-Betting', // 3-betting scenarios
  DEFENDING_BB = 'Defending BB', // Big blind defense scenarios
  CALLING_RANGES = 'Calling Ranges', // Calling vs opens scenarios
}
